<%= form_for(@natter) do |f| %>
  <% if @natter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@natter.errors.count, "error") %> prohibited this natter from being saved:</h2>

      <ul>
      <% @natter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :from %><br />
    <%= f.text_field :from %>
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
